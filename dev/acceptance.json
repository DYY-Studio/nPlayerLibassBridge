{
  "device": {
    "model": "iPhone SE (3rd Gen)",
    "os": "iOS 17.7.2",
    "install": "LiveContainer 3.7.2 Stable"
  },
  "invalidated": [
    "Every result recorded before 2026-09-26 was produced with a dispatch payload that never activated: the dladdr result check was inverted (success was treated as failure) and the basename scan stopped at the first slash, so each unit state stayed OLD and the app always rendered through its bundled libass 0.13.7. Any 'state NEW' in those notes was an assumption, never a runtime measurement -- the only device evidence was that subtitles rendered, which the silent fallback satisfies as well.",
    "The packaged-main SHA-256 values those entries quote (19d3447..., fb6013ca..., 24c940a6...) belong to the broken payload and no longer reproduce. They are kept below only as history.",
    "the `ffmpeg-core` unit is affected by the same pre-fix payload, so the AV1, container, decode, https and snapshot observations recorded for it describe the app's own FFmpeg 4.4.5. The https conclusion in `dev/ffmpeg-core-enable-set.md` and in the `ffmpeg-core-multiunit` entry was not experimentally established when it was written; the gap was measured live afterwards and is recorded by the `tls-fix` and `ffmpeg-core-multiunit-rerun` entries"
  ],
  "reproducibility": [
    "the patched main for a given dylib selection is byte-stable. After the 2026-09-26 dispatch fix the anchors are: `--dylib libass` -> e84ef5b5e10cb10940ecffe73c3509f932a4aa6d2cba053052a7d9e7549792fe, `--dylib ffmpeg` -> a5243f0a36baf5ef5209d51f312bd9d6f0c8d3b05d4053fcbbaa48735339f83b, libass + ffmpeg -> 3bee29d20c4cc6e5979f594dc8df34a6c0fd96e48240e1c2d6a0065fe69be810",
    "the bridge dylibs themselves are NOT byte-reproducible: `make deps` rebuilds the static closures from scratch and the resulting archives (and therefore the linked dylibs) differ on every run. The contract for a shipped dylib is its verification report (exports, install name, dependencies, no initializers, no host paths), not a hash",
    "the IPA sha256 is not a stable anchor: the zip records file mtimes, so re-patching the same input yields a different artifact digest while packaged_main_sha256 and the dylib hashes stay the same. Use packaged_main_sha256 for regression anchors.",
    "adding the `ffmpeg-core` unit extends the anchors: `--dylib libass` stays e84ef5b5e10cb10940ecffe73c3509f932a4aa6d2cba053052a7d9e7549792fe and libass + ffmpeg stays 3bee29d20c4cc6e5979f594dc8df34a6c0fd96e48240e1c2d6a0065fe69be810, while the default selection, which installs all three units, is 638c00d9602b2797d3f18030ebc6ada4bf825a3f871f2f549374fbdecddcdd78"
  ],
  "results": [
    {
      "variant": "baseline",
      "result": "not device-tested",
      "notes": [
        "clean IPA plus the two NOP guards; the behaviour the user already runs today"
      ]
    },
    {
      "variant": "weak-load-only",
      "result": "pass",
      "notes": [
        "starts and behaves like the baseline with zero redirected calls; still valid, this is the shape the app actually ran in every pre-fix entry"
      ]
    },
    {
      "variant": "fallback",
      "result": "pass",
      "notes": [
        "OLD path, no regression; both NOP behaviours intact; still valid, and it is what the 'bridge' entry below was unknowingly measuring"
      ]
    },
    {
      "variant": "bridge",
      "result": "invalid",
      "notes": [
        "INVALID: the payload never activated (the unit state was OLD, not NEW); see the invalidated section",
        "state NEW; SRT, embedded ASS, Matroska embedded fonts and font cache all normal",
        "continuous playback normal, including the subtitle track update path"
      ]
    },
    {
      "variant": "smoke",
      "result": "pass",
      "notes": [
        "SMOKE: PASS, non-empty ASS_Image list, clean app-compatible teardown; this links the dylib directly, so it never exercised the dispatch and is unaffected by the bug"
      ]
    },
    {
      "variant": "libass-multiunit",
      "result": "invalid",
      "notes": [
        "INVALID: same silent fallback as `bridge`; the 21 static checks passed but none of them observed the runtime state",
        "clean IPA re-patched after the multi-dylib refactor with `npa-patch --dylib libass`; 21 static checks pass",
        "packaged main sha256 19d3447193bcd66e03b850876a1281c4bceac087dd50cf6db534e0527fb3a887, i.e. identical to the accepted `bridge` variant; the payload text region matches byte for byte",
        "device: SRT, embedded ASS, Matroska embedded fonts, font cache and continuous playback all normal, with the rebuilt LibASSBridge.dylib",
        "regenerated after the swscale fix; packaged main is still 19d3447193bcd66e03b850876a1281c4bceac087dd50cf6db534e0527fb3a887, i.e. byte-identical to the device-accepted artifact, because the libass unit is untouched by the fix"
      ]
    },
    {
      "variant": "ffmpeg-multiunit",
      "result": "invalid",
      "notes": [
        "INVALID: the ffmpeg units fell back exactly like libass, so the app kept its own FFmpeg 4.4; the sws_alloc_context work below never ran",
        "clean IPA patched with `npa-patch --dylib ffmpeg`; 21 static checks pass",
        "packaged main sha256 fb6013cad491a3548cf55386268f80cdbd794e8ea8cba6ed66b462a595799d55",
        "the changed-instruction check is exact: only this unit's 20 call sites moved, the two libass NOP guards and all 16 libass call sites are untouched, and only LibFFmpegBridge.dylib is weak-loaded",
        "includes the sws_alloc_context fix: the libswscale domain now redirects all five sws APIs the app uses (alloc_context/getContext/getCachedContext/scale/freeContext), so media::VideoRendererImpl's context at this+0x98 is allocated, used and freed entirely inside the new library. Before the fix the app's 4.4.5-allocated SwsContext was handed to the new swscale 9.0.2 (out-of-bounds reads and a mismatched free); that path is reached whenever the video render needs scaling or a pixel-format change",
        "user re-ran this artifact on the device after the sws_alloc_context fix and reported no visible problems with normal operation (playback, scaling, snapshot, speed change, audio)"
      ]
    },
    {
      "variant": "multiunit-both",
      "result": "invalid",
      "notes": [
        "INVALID: both units fell back; the 28 static checks passed but none of them observed the runtime state",
        "clean IPA patched with the default selection (libass + ffmpeg); 28 static checks pass",
        "packaged main sha256 24c940a6111fda140ce382db9ce49b5a37f8e9c94f746a3eb03da9bf8d91b2a7; both dylibs present exactly once",
        "includes the sws_alloc_context fix: the libswscale domain now redirects all five sws APIs the app uses (alloc_context/getContext/getCachedContext/scale/freeContext), so media::VideoRendererImpl's context at this+0x98 is allocated, used and freed entirely inside the new library. Before the fix the app's 4.4.5-allocated SwsContext was handed to the new swscale 9.0.2 (out-of-bounds reads and a mismatched free); that path is reached whenever the video render needs scaling or a pixel-format change",
        "user re-ran this artifact on the device after the sws_alloc_context fix and reported no visible problems with normal operation (playback, scaling, snapshot, speed change, audio)"
      ]
    },
    {
      "variant": "dispatch-fix",
      "result": "pass",
      "notes": [
        "root cause: `npabridge/payload.py` emitted the dladdr result check as `cbnz w0, publish_old`, but dladdr reports success with a non-zero result, so a successful lookup was treated as a failure; the basename scan also stopped at the first '/', so an absolute dli_fname could never match its basename. Both were present since the first payload commit b146213, so the dispatch had never activated",
        "fix: test the dladdr result with CBZ and compare the final path component (the scan reaches the NUL, then steps back over the basename and requires the preceding byte to be '/'). `npabridge.payload` now asserts both shapes on every assembly",
        "runtime proof: macOS PlayCover, iPhone-SE-class app, installed bundle carrying the fixed payload. Before the fix the libass unit state word read 3 (OLD) while LibASSBridge.dylib was loaded and dlsym returned its symbol; after the fix the three unit state words read 2 (NEW) and every slot holds a bridge address",
        "libass 0.17.5 behaviour confirmed visually: the \\kt probe renders the 0.17 karaoke behaviour (libass only added the \\kt override tag in 0.17.0), where the bundled 0.13.7 ignores it",
        "new packaged-main anchors: libass e84ef5b5e10cb10940ecffe73c3509f932a4aa6d2cba053052a7d9e7549792fe, libass + ffmpeg 3bee29d20c4cc6e5979f594dc8df34a6c0fd96e48240e1c2d6a0065fe69be810; the full suite (71 tests, 168 subtests) passes",
        "device-confirmed afterwards: the fixed artifact was installed on the device and the \\kt probe renders the 0.17 karaoke behaviour there as well, with state NEW for every unit"
      ]
    },
    {
      "variant": "pixfmt-fix",
      "result": "pass",
      "notes": [
        "root cause: the app's FFmpeg 4.4 still numbers three AVPixelFormat members that 9.0.2 removed (AV_PIX_FMT_VAAPI_MOCO, AV_PIX_FMT_VAAPI_IDCT, AV_PIX_FMT_XVMC) and they sit inside the enum, so every value from legacy 46 on is shifted (+2 up to 153, +3 after). The swscale shims forwarded the app's value straight through, so AV_PIX_FMT_P010LE (legacy 161) was read by 9.0.2 as AV_PIX_FMT_GBRAP12LE (4 planes, 16-bit)",
        "symptom before the fix: P010/HEVC folder thumbnails rendered garbage on macOS (lower half green, no crash) and crashed on iOS; 8-bit H.264 was unaffected because NV12 (23), YUV420P (0) and the RGB24 destination (2) are not renumbered",
        "fix (commit 05423b2): npa_modern_pixfmt() translates src and dst in npa_sws_getContext and npa_sws_getCachedContext. The three dropped members and any value outside the 4.4 enum become AV_PIX_FMT_NONE so sws_getContext fails instead of scaling with a format that means something else. The magic numbers (46, 153, 198 and the dropped-before counts 2 and 3) are named constants documented in the source and the commit message",
        "redirect coverage re-audited after the sws_alloc_context miss: the app calls only the five redirected swscale entry points and never sws_setColorspaceDetails/sws_getColorspaceDetails/sws_init_context. The unredirected swr_close/swr_is_initialized callers are the embedded Opus decoder using its own 4.4.5 contexts, so no SwrContext crosses between the app wrapper (fully redirected) and libavcodec; see notes/ida-investigation-5-swscale-swr-calls.md",
        "device result (`--dylib ffmpeg`): P010/HEVC folder thumbnails correct and no crash; continuous playback normal; H.264/AVC unchanged; HDR playback normal; audio normal",
        "the slight HDR tone-mapping difference is expected: the app never calls sws_setColorspaceDetails and routes its own Color Space setting only to the renderer/display layer, so HDR conversion uses swscale 9.0.2's own defaults instead of 4.4.5's. Same class of accepted pixel-level difference as the libass 0.13 to 0.17 rendering change",
        "packaged main sha256 a5243f0a36baf5ef5209d51f312bd9d6f0c8d3b05d4053fcbbaa48735339f83b (ffmpeg only) and 3bee29d20c4cc6e5979f594dc8df34a6c0fd96e48240e1c2d6a0065fe69be810 (both); shipped LibFFmpegBridge.dylib 249831a185bf4f306589e781b2de87c55398493ef3327c625d0853cfa02ebd6e. The full suite (73 tests, 224 subtests) passes",
        "this is the first time the ffmpeg unit is actually exercised: every earlier ffmpeg entry above fell back to the app's own 4.4.5"
      ]
    },
    {
      "variant": "tls-fix",
      "result": "pass",
      "notes": [
        "symptom on the device, three-unit selection only: an https M3U8 HLS does not play - the hardware decode path fails and falls back, the software path shows no duration or other stream data. The QuickTime engine connects (the Apple stack has its own TLS). The libass+ffmpeg selection plays the same stream, because it keeps the app's own FFmpeg",
        "isolation on the same artifact and source: the same HLS over http plays, and an https direct URL plays (that path goes through the app's curl stack, not FFmpeg). Together with the enable-set comparison - whose only app-side capability the unit lacked was TLS - this pins the failure to the missing TLS backend",
        "root cause: the unit was configured with --disable-autodetect and no TLS option, so CONFIG_TLS_PROTOCOL and CONFIG_HTTPS_PROTOCOL were 0 and libavformat carried no ff_tls_protocol/ff_https_protocol. Any https open through FFmpeg's protocol layer failed with 'https protocol not found', where the app's 4.4.5 (OpenSSL TLS) succeeds",
        "this corrects the https conclusion recorded for the ffmpeg-core-multiunit artifact, which was measured on the pre-fix payload and therefore described the app's own FFmpeg; the invalidated section already voided that entry",
        "fix: deps/ffmpeg-core.lock.json configures the closure with --enable-securetransport and adds -framework Security to the system link args (Apple's TLS, no new dependency; the app uses OpenSSL, so the backend differs by design). config.h now reads CONFIG_TLS_PROTOCOL 1, CONFIG_HTTPS_PROTOCOL 1, CONFIG_SECURETRANSPORT 1, and libavformat.a carries tls.o and tls_securetransport.o with ff_tls_protocol and ff_https_protocol",
        "evidence: make verify 21/21 (the dylib's dependencies stay on system frameworks), uv run pytest 75 passed / 1621 subtests, dev/tests 7 passed. Packaged main is unchanged for every selection; signed dylib sha256: LibFFmpegCoreBridge eb07d4fdf056ecf702d6cac0b29fbf8ebc7d445b33ecccdcb15a96f10f6b857c -> ca99bbe3f25e50852d7f1b5fb03fed326ba9f848f76175e17a17ecdec8e1e7bb, LibASSBridge and LibFFmpegBridge unchanged",
        "still open (rtmps): the same backend serves rtmps, but no rtmps stream was reported in this pass. SecureTransport is a different implementation than the app's OpenSSL (deprecated API, protocol version negotiated by the system, certificates from the system trust store), so its behaviour is measured rather than assumed",
        "device, 2026-09-26: https HLS passes on the three-unit selection, where it failed before the fix and where the same source over http already passed"
      ]
    },
    {
      "variant": "ffmpeg-core-multiunit",
      "result": "invalid",
      "notes": [
        "INVALID: every observation recorded here was measured with the pre-fix dispatch, so it describes the app's own FFmpeg 4.4.5 rather than the unit (see the invalidated section). The unit's own device pass is recorded in the `ffmpeg-core-multiunit-rerun` entry below",
        "clean IPA patched with the default selection; three dylibs weak-loaded, 35 static checks pass",
        "packaged main sha256 4d7e79ba3d2a6a1afaa68948002ee3da36ed9c33cf1e7801df122539572b2272 (pre-fix payload)",
        "bridge sha256: LibASSBridge b2db1e1baa7b154d145b6e6bbc6b84928bb1fc348c9c7c68588b63e5b0acc12b, LibFFmpegBridge 075c68cdaf2b0dd5ef9ff5e6755d2d045185c473de378c66bf517d35f4a37af7, LibFFmpegCoreBridge eb07d4fdf056ecf702d6cac0b29fbf8ebc7d445b33ecccdcb15a96f10f6b857c",
        "this artifact is where the app's statically linked FFmpeg 4.4.5 becomes 4.4.8: libavformat, libavcodec, libavutil and libswresample all come from the unit, so every container is demuxed by it and it is the software decoder of last resort",
        "the observations recorded at the time - AV1 software decode, the containers, FFmpeg-decoded audio and video, the software fallback, seek, continuous playback, speed change, subtitles, HTTP direct, HLS, snapshots - all belonged to the app's own FFmpeg, and the https conclusion was wrong: the gap was live on the fixed payload, as the `tls-fix` entry records"
      ]
    },
    {
      "variant": "ffmpeg-core-multiunit-rerun",
      "result": "partial",
      "notes": [
        "artifact: the default selection rebuilt after the three fixes. Packaged main 638c00d9602b2797d3f18030ebc6ada4bf825a3f871f2f549374fbdecddcdd78 (unchanged from the dispatch-fix rebuilds); signed dylibs LibASSBridge b2db1e1baa7b154d145b6e6bbc6b84928bb1fc348c9c7c68588b63e5b0acc12b, LibFFmpegBridge e1b8f3d1571f3d3274c08d6c0872bff8086d16fabbac268c213b7643e9467b51, LibFFmpegCoreBridge ca99bbe3f25e50852d7f1b5fb03fed326ba9f848f76175e17a17ecdec8e1e7bb",
        "device-verified in this pass: https HLS playback (what the TLS fix targets), P010/HEVC on both decode paths - hardware and software (what the pixfmt fix targets), AV1 software decode through libdav1d, and audio decode",
        "also device-verified in the same session: the subtitle matrix (SRT, embedded ASS, Matroska embedded fonts, the font cache), the containers mkv/webm, mp4, mpeg, ts and wmv, software decode of mpeg1/vp8/wmv3, continuous playback, seek, thumbnails and speed change",
        "liveness: for the ffmpeg and ffmpeg-core units the evidence is behavioural - the swscale, libavformat and libavcodec paths were demonstrably live when they failed (P010, https), and both fixes only take effect inside those units. For libass the subtitle matrix alone would not settle it (the bundled 0.13.7 renders the same subtitles), so the user ran the \\kt probe - behaviour that only 0.17 has and 0.13.7 ignores - and saw the 0.17 behaviour. All three units are therefore confirmed live on this artifact",
        "not measured in this pass: the recording/mux path (the HLS session muxers, encoders and bitstream filters), rtmps/rtsp, the whole-unit fallback proof, and the explicit state-word read (liveness above is behavioural, not a read of the state word). The entries that used to carry the older items are marked invalid above",
        "result is partial rather than pass: the fixes and the whole playback matrix pass, but the fallback proof and the recording path are A5 criteria and are still open"
      ]
    }
  ]
}
