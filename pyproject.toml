[project]
name = "nplayer-libass-bridge"
version = "0.1.0"
description = "Host tooling for the nPlayer libass bridge prototype"
requires-python = ">=3.11,<3.15"
dependencies = ["lief==1.0.0"]

[dependency-groups]
dev = ["meson", "ninja", "pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
